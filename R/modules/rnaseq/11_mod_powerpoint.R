#' Generate RNA-seq PowerPoint summary (stub)
#'
#' Placeholder for PowerPoint report generation.
#' Returns NULL (no file) until officer-based implementation is added.
mod_rnaseq_powerpoint <- function(pre, qc_pre_obj, de_res, clustering_obj,
                                   pathway_res, config, out_dir) {
    message("[mod_rnaseq_powerpoint] PowerPoint generation not yet implemented — skipping.")
    character(0)
}
